## 纠偏算法逻辑文档

### 1. 算法逻辑
1. 根据雷达检测获取到雷达坐标系下托盘的当前位置姿态，计算出叉车坐标系，以及在叉车坐标系下托盘的位置姿态。
2. 将叉车当前位置姿态（叉车前轮中心）固定为原点进行规划计算。
3. 根据托盘的位置，计算出托盘前`2m`处的位置作为目标点。
4. 判断叉车是否前进（叉齿背向方向为正向，即前进方向）。叉车直接旋转至目标点位，设为 $\alpha$，叉车前进`0.5m`再旋转至目标点位，设为 $\beta$。若 $\alpha > \beta$ 且 $ \alpha > 30º$，或者当前时刻雷达检测到托盘距离小于`2m`，则需要前进`0.5m`，然后旋转至目标点位，否则，直接旋转至目标点位。
5. 旋转至目标点位后，再行驶至目标点位，最后旋转至目标点位的姿态即可。
6. 算法一共返回≤4个点，分别是初始点位、（前进点位）、旋转点位和目标点位。


### 2. 输入输出
### 输入
- 叉车坐标系下托盘位姿
- 雷达系下距离托盘距离

### 输出
- ≤4个点位
